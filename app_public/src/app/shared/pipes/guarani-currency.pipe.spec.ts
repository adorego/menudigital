import { GuaraniCurrencyPipe } from './guarani-currency.pipe';

describe('GuaraniCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new GuaraniCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
